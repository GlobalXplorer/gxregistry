const express = require('express');
      router = express.Router();

const AuthController = require('../../controllers/auth');

// TODO: Generate and provide clients with API keys and tokens
// router.post('/', [
//   AuthController.post
// ]);

module.exports = router;
